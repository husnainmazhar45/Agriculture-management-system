import 'package:flutter/material.dart';

class SyngentaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(
          title: Text('About Syngenta'),
        ),
      ),
    );
  }
}
